<h1>Listing scenarios</h1>
<% if @scenarios.count.zero?%>
<div>
	No scenarios in database. Please seed data using console
	<p>
	<code>rake db:seed</code>
	</p>
</div>
<% else %>
<table>
  <tr>
    <th>Title</th>
    <th>Scenario type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @scenarios.each do |scenario| %>
  <tr>
    <td><%= scenario.title %></td>
    <td><%= scenario.scenario_type %></td>
    <td><%= link_to 'Show', scenario %></td>
    <td><%= link_to 'Edit', edit_scenario_path(scenario) %></td>
    <td><%= link_to 'Destroy', scenario, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>
<% end %>
<br />
<div>Add new scenario using seed.db file</div>
<br />
<%= link_to 'See routing rules', rounting_rules_url %>
